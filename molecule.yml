---
vagrant:
  platforms:
    - name: centos-7
      box: viniciusfs/centos7
  providers:
    - name: virtualbox
      type: virtualbox
      options:
        memory: 512
        cpus: 1
  instances:
    - name: ntp
      ansible_groups:
        - group1

ansible:
  playbook: tests/playbook.yml

verifier:
  name: testinfra
